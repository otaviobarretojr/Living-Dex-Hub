/* Living Dex Hub — F6.3 verified encounter guide */
(()=>{'use strict';
const q=(s,r=document)=>r.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function ownedLetsGo(){try{return window.state?.ownedGames?.letsgo!==false}catch(e){return true}}
function methodPt(x){return({Gift:'Presente',Walking:'Caminhando',Special:'Encontro especial',Surfing:'Surfando',Flying:'Voando',Trade:'Troca'})[x]||x}
function versionPt(v){return v==='pikachu'?"Let's Go Pikachu":v==='eevee'?"Let's Go Eevee":v}
function render(){const guide=q('#ld8ldGuide'),body=q('#ld8ldGuideBody');if(!guide||!body||guide.hidden)return false;const id=Number(guide.dataset.pokemonId)||0,data=window.ld8LetsGoEncounters?.get?.(id);let old=q('#ld8ldVerifiedEncounters',body);if(old)old.remove();if(!data||!ownedLetsGo())return false;const section=document.createElement('section');section.id='ld8ldVerifiedEncounters';section.className='ld8ld-verified-encounters';section.innerHTML=`<div class="ld8ld-verified-title"><div><small>DADOS CONFIRMADOS</small><b>Let's Go Pikachu / Eevee</b></div><span>${data.encounters.length} registro${data.encounters.length===1?'':'s'}</span></div><div class="ld8ld-verified-list">${data.encounters.map(e=>`<article><div class="ld8ld-verified-main"><b>${esc(e.location)}</b><small>${esc(methodPt(e.method))}</small></div><div class="ld8ld-verified-meta"><span>Nv. ${e.levelMin===e.levelMax?e.levelMin:`${e.levelMin}–${e.levelMax}`}</span>${e.rate!=null?`<span>${esc(e.rate)}% encontro</span>`:''}<span>${e.versions.map(versionPt).join(' · ')}</span></div>${e.note?`<p>${esc(e.note)}</p>`:''}</article>`).join('')}</div><small class="ld8ld-verified-source">Base local validada a partir da análise estática do DataDex. Informações parciais são identificadas, sem completar lacunas por suposição.</small>`;body.appendChild(section);return true}
function install(){if(typeof MutationObserver==='undefined')return;const obs=new MutationObserver(()=>setTimeout(render,0));obs.observe(document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['hidden','class','data-pokemon-id']});document.addEventListener('click',e=>{if(e.target.closest?.('#globalGrid .ld8ld-poke[data-id]'))setTimeout(render,40)},true);setTimeout(render,500)}
window.ld8EncounterGuideRender=render;window.ld8EncounterGuideAudit=()=>({version:'8.0-f6.3',letsGoSeed:!!window.ld8LetsGoEncounters,verifiedOnly:true,partialDataExplicit:true,location:true,method:true,level:true,encounterRate:true,versionSpecific:true});
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
})();
